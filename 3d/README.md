# Mini Robot Dog — V1 printed CAD

Parametric 12-DOF quadruped built around **12 × Feetech/Waveshare ST3215 (STS3215)** bus
servos. Everything structural is FDM-printed (PETG/ASA); no aluminium, no machining, no
external bearings — per `mini_dog_codex_handoff.md`.

The model is generated by [mini_dog.py](mini_dog.py). Re-run it after changing any parameter:

```bash
.venv/bin/python mini_dog.py     # writes out/step, out/stl, out/bom.json
.venv/bin/python render.py       # writes out/view_*.png
.venv/bin/python fea.py --all    # strength check - run after every change
.venv/bin/python export_sim.py --check   # writes out/sim/ (URDF + MJCF, flat + terrain)
```

Any change to the model is re-checked for strength **and** re-exported to simulation in the
same pass; see the workflow in [CLAUDE.md](CLAUDE.md).

There is a fourth script, and it builds hardware rather than robot:

```bash
.venv/bin/python servo_bench.py           # writes out/bench/{step,stl}, out/bench/bom.json
```

Three printed stands that **measure** the ST3215 — stall torque, joint compliance,
backlash, step response at known inertia, and foot force on a whole leg — so leg motion can
be found by searching joint space instead of solved by the analytic IK in
`../ros2/smalldog_walker`. Every actuator number in this repo is a vendor figure today
(`SERVO_STALL_NM`, `SERVO_NOLOAD_RADS`, all four `MJ_*`), and a search filtered against
vendor figures is a search over a robot that does not exist. Spec, BOM and protocols:
[BENCH.md](BENCH.md). It imports `mini_dog.py` and adds nothing to the robot, so it is not
part of the strength/sim loop above.

## Servo interface — measured, not guessed

Downloaded from the Waveshare ST3215 wiki into [ref/](ref/) and measured directly with
the scripts in [tools/](tools/):

| | value | source |
|---|---|---|
| case | 45.22 × 24.72 × 35.00 mm | `ST3215.step`, `ST3215.pdf` |
| rotation axis | along the **35 mm** dimension, 10.11 mm from the output-side end face, centred in the 24.72 width | drawing SCS215 |
| driven hub plate (25T side) | ⌀19.2, outer face **+20.30** from the case mid-plane | STEP |
| passive hub plate | ⌀19.2, outer face **−16.95** (recessed 0.55 into the case base, ⌀25 pocket) | STEP |
| hub-to-hub mounting span | **37.25 mm** | drawing |
| hub fixing | 4 × ⌀2.5 **tapped M2.5** on a **⌀14** bolt circle, at 0/90/180/270° | STEP |
| hub plate thickness | driven **2.50** (z 17.80…20.30), passive **2.20** (−16.95…−14.75) | STEP |
| case side faces | **no threaded holes** — the case is gripped by form, not by screws | STEP |

That last row drove the whole architecture.

## Joint architecture

```
proximal link ──► SLEEVE : rectangular tube around the servo case, open on the axis.
                          Reaction torque goes into the 45×35 flats, not into screws.
                          Two M3 thrust bolts through its back wall take up the play.
distal link   ──► FORK   : two arms lying on the two aluminium hubs, 4 × M2.5 each.
                          The arms straddle the sleeve, so the servo is also captured axially.
```

Consequences:
- the servo is **never** loaded through a printed thread or a single-shear horn;
- assembly = slide the servo into the sleeve, set the two thrust bolts, bolt the fork to both hubs;
- the stock passive hub is used exactly as supplied — no 628/685 bearings (per spec).

### The thrust clamp

A form fit needs clearance to assemble, and clearance is backlash: on `CLR` alone the case
can rock ~±0.9° in its sleeve, ~±2.5° at the foot once three joints stack up, and every
gait reversal works that gap into the PETG.

So two M3 jack screws go through the sleeve's back wall (the −x end, away from the cable
window) at y = ±5.5 and push the case forward onto the two legs the cable window leaves
standing in the front wall. That **pins** the case rather than gripping it: turn it one
way and the case drives itself onto one thrust bolt at the back while the opposite front
leg stops it at the front; turn it the other way and the other pair does the same. The
play is closed by geometry, not by friction — which is why there are two bolts and not
one. Each threads into an M3 nut in a side-loaded channel in the lug, so nothing threads
into plastic. **Fit M3 × 10 and no longer** — see the radius rule below.

The obvious clamp is to slit the sleeve into a C and squeeze it, and it is the wrong one
here. The −x end wall is the tube's **only** crossing of y = 0 — the cable window has
already eaten the front one — so slitting it opens the entire sleeve-plus-link box section.
Measured, not argued: the C costs `thigh_A` **1.1 → 0.6** inter-layer SF at servo stall and
2.65 → 12.2 mm of deflection, and tying the front wall back across the window does not buy
it back (0.5). The thrust clamp leaves the ring intact and costs nothing.

Everything either clamp may add has to stay inside **r < 23 mm** of the joint axis: the
distal fork's spine sweeps the annulus 23…34 over the sleeve's whole length, and the hip
bracket's inboard web already comes to r = 22.0. The lug corner sits at 21.6, and a screw
longer than 10 mm puts its head outside that. The ROM scan is unchanged by the clamp —
roll ±90, pitch ±90, knee ±110 with and without it.

Range of motion is measured by swept boolean interference against the real solids
(`rom_scan()` in the script), not estimated.

## Geometry

| | mm |
|---|---|
| chassis tray | 126 × 92 × 50, walls 2.8, floor 3.0 |
| deck | 4 mm plate + 6 mm side lips |
| wheelbase / track | 180 / 152 |
| hip-roll axis | X ±90, Y ±36, Z 0 (axis along X) |
| hip-pitch axis | X ±90, Y ±76, Z −30 (axis along Y) |
| thigh / shin | 75 / 82 |
| foot | ⌀26 TPU dome |
| joint width (over fork arms) | 46.2 |
| shin section, boss / shaft / ankle | 27.6×45.8 / 24×30 / 19.5×20.5 |

The hip-pitch axis deliberately sits **30 mm below** the hip-roll axis: that lifts the
roll fork out of the thigh's swing plane and is what buys ±80° of hip pitch instead of ±15°.

### Shin profile

The shin is a lofted, hollow, curved blade, not a taper. Its shape comes from the
**Waveshare DOG PRO lower leg** that ships in [ref/](ref/) — the one real quadruped link
this repo owns — measured by [tools/ref_ws_shin.py](tools/ref_ws_shin.py):

| | reference | ours |
|---|---|---|
| joint centres | 101.8 mm | 82 mm |
| thickness along the joint axis | **12.0 mm, constant** | 20 mm, near-constant |
| depth in the bending plane | 26.3 → 8.9 mm (×0.34) | 27.6 → 18.6 mm (×0.67) |
| centreline bow off the chord | 5.3 % of length | 5.4 % of length |

So it is a **blade**: deep in the plane the knee bends it, thin along the joint axis, and
curved. Ours keeps the proportions, not the absolute sizes — PETG is roughly 40× less
stiff than the aluminium that part is milled from, so the blade stays much fatter.
[tools/ref_calf_profile.py](tools/ref_calf_profile.py) cross-checks the same shape against
Unitree A1/Go1/Go2/B2, the MIT mini cheetah and Spot.

The whole profile is the `SHIN_PROFILE` table in [mini_dog.py](mini_dog.py) — u below the
knee axis, fore-aft depth, lateral thickness, centreline bow, corner radius — interpolated
with a monotone cubic and lofted. Things that bite:

- the interpolation is **monotone** on purpose. A curve that overshoots its control points
  makes the loft spline bulge where nothing asked it to;
- the boss section sits 0.2 mm inside the fork on every face, and starts *inside* the fork
  spine, so the union has no coincident faces to trip over;
- the shear web is left standing by taking a slab out of the cavity, and that slab stops
  0.5 mm short of the cavity's end caps. A tool crossing those spline faces makes the OCC
  boolean silently return its own input — the part then comes out webless *and* with a
  shell that later cuts fail on just as quietly. `isValid()` does not catch it; check the
  section areas;
- the cable-tie anchor goes through the **lateral** wall, which is the neutral axis for the
  bending the knee actually does. On the fore-aft face it would sit on the extreme fibre
  and cost ~13 % of the section modulus.

## Payload bays

- 3S2P 6 × 21700 (Molicel P42A class): cradle for two layers of three, cells along X,
  four fins on the cell pitch, two strap slots. Sits on the floor, centred → low CG.
  Every dimension is derived from `CELL_D`/`CELL_L` — the P42A's datasheet maxima over the
  wrap — so the three channels come out at exactly `CELL_D + CELL_FIT` = 21.7 mm, with
  `BATT_TAB` = 4 mm at each cell end for the welded nickel and 3.6 mm over the pack to the
  deck. It was a literal 67 mm wide until 2026-08-28, which left a 19.5 mm channel for a
  21.3 mm cell: the pack it was drawn for did not exist. Do not put the width back.
- 3S BMS: bay in the front, ESP32 + Feetech URT-1 bay in the rear.
- Rear connector panel, around the bus window: **XT60** (master disconnect / bench supply,
  on the fused P+), **XT30** (charge) and a pass-through for the 3S JST-XH balance lead.
  The two XT shells are different sizes on purpose — a charger cannot be plugged into the
  servo bus. Each XT sits in a pocket in a locally thickened wall and goes in **from
  inside, before the deck**; the outer 1.5 mm of wall is left as a 0.8 mm lip all round and
  that lip takes the unplug force, so the mating half stands 1.5 mm proud. The XT60 is
  below the window rather than beside it because the strip between the window's edge and
  the rear deck boss is 16.2 mm and an XT60 needs 16.5. Size the charge lead for 5 A
  (18 AWG) and the bus lead for the 30–35 A fuse (14 AWG).
- Orange Pi 5 Pro: four printed standoffs on the deck (92 × 54 pattern — **verify**).
  With that pattern the rear pair lands at x = −68 on a deck that ends at −63, i.e. 0.8 mm
  clear of it, so `chassis_top` currently comes out as three solids. That is the hole
  pattern being wrong, not the deck: fix `OPI_HOLES` / `OPI_X` once a real board is measured.
  Its **envelope** is a separate number and a load-bearing one: `OPI_BOX` = 100 × 62 × 20
  over the deck (board, connector row, heatsink), the box every exporter hangs
  `ELECTRONICS_KG` on *and* the keep-out `gps_mount` arches over. It used to be a literal
  on each side and they had already drifted — 92 × 62 × 20 here against 100 × 62 × 18 in
  the ROS 2 generator. **verify** it with the hole pattern.
- LiDAR: a **tilted seat**, not a mast. The Unitree L1/L2 scan a hemisphere *above*
  themselves — vertical FOV runs from the sensor's own base plane to its axis, and NEGA
  mode (the L2's factory default) buys 6° below that plane and nothing else. So height
  buys no field of view at all: standing upright on the old 44 mm pedestal the L2 met the
  ground 2.5 m in front of the dog, and the pedestal was 38 mm of raised centre of gravity
  bought for nothing. The seat now leans **45° forward** (`LIDAR_TILT`), which is where the
  sensor's densest ring — the centre of its vertical FOV — lands on the horizon, so the
  things that stop a walking robot get sampled best while the lower rim reaches the ground
  **147 mm in front of the leading foot** in NEGA (194 mm without it).
- `LIDAR_SEAT_Z` is **derived, not styled**: no static bodywork may sit above the seat
  plane, i.e. `z + (x − LIDAR_X)·tan(tilt) < LIDAR_SEAT_Z` for every body point. The deck's
  own stiffening lip at (63, 35) binds it, giving 56.0; 60.0 keeps 4 mm of margin and
  leaves 31 mm under the seat for the L2's RJ45 to turn down into the ⌀22 core.
  `mini_dog.py` re-checks that inequality on every run and prints `lidar clear:` or
  `!! LIDAR FOV`, the same way it prints `body clear:`. Only *static* parts count — the
  legs sweep the forward-down cone every stride, and no mount geometry can fix that; they
  get masked in software, as they do on every quadruped.
- `LIDAR_X` is **pinned at 42 and is not a free choice**: the base disc must clear the
  Orange Pi standoffs at (24, ±27) behind it (≥ 38.8) and the pedestal's own deck bolts
  must land on a deck that ends at 63 (≤ 44.1). There is nowhere to move it to fore-aft.
- Two bolt circles, deliberately different. Ours is ⌀45 at 45°, every screw under a leg.
  The sensor's is **⌀51 at 22.5° + k·90°, 4 × M3 tapped 6 mm deep**, normal to the seat —
  measured off the L2 mechanical drawing, together with its ⌀60 spigot and ⌀75 base. They
  cannot be merged: ⌀51 for the *deck* screws needs the base disc at r = 30, which is the
  Orange Pi standoff clash the disc was shrunk to 26 to avoid.
  Its base disc is ⌀52, not ⌀60: at ⌀60
  it and the Orange Pi standoffs shared 95 mm³ of solid. `LIDAR_X` is one constant now,
  read by both `chassis_top` (the bolt circle) and `lidar_mount`, and `mini_dog.py`
  intersects the three body parts against each other on every run — an `!! INTERFERENCE`
  line is a failure exactly like `!! INVALID`.
- **There is no LiDAR guard, and that is a decision.** There was one — a low bow off the
  deck's front screws, living in the wedge underneath the cone — and it is gone. Two
  reasons, and the second is the honest one. First, you cannot cage this sensor anyway:
  its FOV is a hemisphere referenced to its own base plane, so every bar above that plane
  is a permanent blind stripe, and the manual is blunt — *"do not block its FOV. Even
  installing a transparent glass plate on the optical window will affect the
  performance."* The only free volume is **underneath** the cone, so a guard could only
  ever be a low bow ahead of the nose, never a cage over it. Second, that bow did not
  protect much: `hip_bracket` already reaches x = 114.3, level with the L2's nose at
  114.5, so the front legs meet a flat wall at the same moment the sensor does, and the
  bow could not cover a bare horizontal edge at the sensor's own height (a table top, a
  shelf) with clear air beneath — guarding *that* means a bar dead ahead, in the cone.
  What it did cover was something reaching below ~205 mm with clear air above it.
  Removing it bought 11.1 g off the very nose (worth more than it sounds — see *Terrain
  feedback* in `../ros2/README.md`), a clear lower half of the camera frame, and two
  M3 × 12 back where the deck's front screws needed M3 × 24. `git log` has the shape if it
  is ever wanted back; it would now have to clear the camera, which occupies the slot its
  inboard arms used to fly through.
- The sensor pose lives once, in `mini_dog.lidar_pose()` / `lidar_com()`. `export_sim.py`
  and the ROS 2 generator both read it from there; each of them used to carry its own
  `42.0` literal and its own guess at the envelope (70 × 70 × 60 against the real
  75 × 75 × 65). The mass box stays axis-aligned while the real sensor leans — exact in
  mass and centroid, ~7 % out on one inertia axis of 9 % of the robot.

### Camera — IMX415 USB module (`camera_mount`)

A Weinan **WN-L2101.K203L**: Sony **IMX415** (1/2.8", 8 MP) behind a fixed-focus M12 lens,
USB 2.0 UVC, digital mic on the board. It is here to put a *name* on what the L2 already
puts a *shape* on — the lidar returns geometry and no identity, and recognising a face is
the one job on this robot that wants pixels instead of points. It was chosen over a 5 MP
OV5693 module for the sensor, not the megapixels: 1/2.8" is ~3× the area of the OV5693's
1/4", and a walking robot needs a short exposure or the face smears.

- **The module is a 90 × 15 × 1.6 mm strip**, not the 38 × 38 square the same seller's
  other modules are, with the optical axis 70.59 mm from the connector end and a ⌀14 M12
  holder standing 16.2 mm off the board. Every one of those is off the vendor's *Product
  Dimensions* drawing, transcribed in [ref/camera/](ref/camera/) together with the two
  readings on it that are **not** certain — `CAM_LENS_U` and whether the catalogue's 90°
  is the diagonal. That shape — a long thin strip with the lens near one end — drives the
  entire mount.
- **There is exactly one place it fits, and the four walls are all measured parts.**
  *Floor*: the hip-roll cradles — `roll_module` is solid over |y| ≤ 14 from x = 63 out to
  x = 106.5 and its root gusset is solid over |y| ≤ 49 from x = 63 to 67.5, both topping
  out at `CAM_LEDGE` = 15.36. *Ceiling*: the LiDAR pedestal's base disc, z = 29…35,
  reaching x = 68. *Back*: the chassis front face at x = 63. *Front*: the hip-roll fork's
  **rear arm** at x = `ROLL_X + FORK_Y0` = 68.1 — that arm is a disc about the roll axis
  and it sweeps everything within r = 34 of it over the roll ROM, so 68.1 is a hard wall.
  The board lives in the 5 × 15 mm slot that leaves, and only the **lens** goes past 68.1
  — it may, because on the centreline its closest approach to either roll axis is 36.3 mm,
  outside the 34 the fork sweeps.
- **The alternatives were built and measured, not argued about.** Landscape at z = 23,
  x = 68…72: hip roll clashes from **+5°** (9.5 mm³) and reaches **224 mm³** by +90°, the
  70.6 mm tail crossing the fork's 23 < r < 34 annulus over y = −64…−31. Under the belly
  at z = −20 / −32 / −45: the front legs sweep it at every height — thigh, shin and foot
  all clash from **+30° of hip pitch**.
- **`LIDAR_BASE_FLAT` = 63.5.** The pedestal's ⌀52 base disc reaches x = 68, which put
  6 mm of pedestal directly over that slot and left 13.6 mm under it for a 15.0 mm board.
  The disc is now flat-cut in front. Nothing needs the material: the deck bolts are on a
  ⌀45 circle whose front pair sits at x = 57.9 and the four pedestal legs at r = 21 reach
  x = 63.35, so a flat at 63.5 leaves the legs untouched and keeps 5.6 mm of rim in front
  of the bolt. It costs 105 mm² of a 2124 mm² disc.
- **`CAM_TILT` = 6°, and that is a fit, not a preference.** The slot is 5 mm deep
  fore-aft and a tilted board eats 15·sin t of it; 6° costs 1.6 mm and leaves ~1 mm of
  wall front and back, 15° does not fit at all. What that buys, honestly: the lens ends up
  **209 mm off the ground** with an **82 × 52°** field, so a 1.6 m face is in frame from
  **2.2 m** out and still ≥ 90 px across — enough to recognise — to about **4 m**. Closer
  than 2.2 m the head leaves the top of the frame. More tilt would fix that and does not
  fit; a shorter board would.
- **The mount is a C-section standing on the gusset's own top face** — a 4.5 mm deep,
  98 mm wide ledge, and the only flat surface anywhere near the camera. It cannot grip the
  *front* of the board's lower half (0.5 mm to the fork arm) and it cannot put a nut behind
  the board (2.9 mm of depth against an M3 nut's 5.85 across corners), so the board slides
  in endwise and is trapped: a slot in the shelf below, a wall in front of its upper half,
  a skirt behind. Both M3 live at the +y end, past the board's short end, where the boss
  can be full size; the far end is keyed by a printed tongue in a pocket, which fastens
  nothing and so needs no nut. The screws come down into nuts in slots in the gusset,
  opening forward — open air under the chin, and the only face still reachable with the
  board in. 4.1 g of part.
- **`camera_clear()` is the camera's `lidar_fov_clear()`.** It reports the highest
  elevation at which a solid still shows inside the frame, and the build prints it per
  part. A 1.6 m face at 2.2…4 m sits at +19…+32° from this lens, so anything reaching into
  that band is in the way of the one job the camera has. With the guard gone every static
  part reads **out of frame**, and MuJoCo's segmentation pass agrees: `export_sim.py
  --check` renders one frame from the model's own `<camera>` to `out/sim/camera_view.png`
  and reports **0.0 % of the frame is the robot**. That render is also the only check that
  catches a wrong quaternion — a camera 90° off still has a plausible pose and a plausible
  fovy.
- **Two frames, and that is not pedantry.** `camera_link` is REP-103 (+X out of the lens,
  +Z up), which makes `CAM_TILT` readable as a pitch about +Y; `camera_optical_frame` is
  REP-145 (+Z out of the lens, +X right, +Y down), which is what every `camera_info`,
  every rectification matrix and every `cv::projectPoints` assumes. They differ by a fixed
  (−90°, 0, −90°). MuJoCo uses a third convention again — it looks down its own −Z with
  +Y up — so [camera.py](camera.py) builds that quaternion from the axes rather than
  writing it out.
- **Marked verify.** `CAM_OPT` (entrance pupil up the axis from the PCB face, 12 mm),
  `CAM_FOV_D` (the catalogue's single number, read here as the **diagonal**), `CAM_RATE`
  and `CAMERA_KG` (12 g — the vendor gives no mass; weigh one). A lens that is really 78°
  or 96° diagonal changes every field-of-view number above.

### GPS — GY-NEO6MV2 + active patch (`gps_mount`)

- A patch antenna is the same kind of sensor as the L2: it looks at the hemisphere **above
  itself**, and anything standing over it is a permanent hole in its view. So it wants the
  highest flat spot on the robot — and this robot has none free. The deck's top face is the
  Orange Pi (`OPI_BOX` spans x −72…28, |y| ≤ 31, z 29…49, i.e. everything between the two
  stiffening lips); ahead of it is the LiDAR pedestal and then the L2; the strips outboard
  of the Pi are 12 mm wide against a 25 mm patch.
- What **is** free is the volume above the Pi and behind the L2, and it is free for a
  reason worth stating: the L2's cone is referenced to its own base plane and that plane
  leans 45° forward, so *up and behind* is **below** it. At the mast's worst vertex
  `z + (x − 42)·tan 45° = −9` against the seat's 60 — 69 mm of margin, where the deck lip
  in front has 4 — and `lidar_fov_clear()` charges the real 96° NEGA cone against the part
  on every build: **+48.1° outside it**.
- So the mount is a trestle on the deck's **rear** pair of boss screws, mirroring what
  the removed `lidar_guard` used to play with the front pair: it drills nothing, those
  two M3 × 12 simply
  become M3 × 24. The receiver lies on the platform and the patch sits on the receiver,
  which is how the module ships.
- **The kink in the arms is derived, not styled.** Two constraints pull opposite ways: no
  run may lean more than 45° off vertical or the part stops printing without support (the
  rule that also shapes `lidar_mount`), and the arm has to be clear of `OPI_BOX`'s top
  corner at (|y| = 31, z = 49) before it gets inboard of it. A straight 45° arm off the pad
  passes that corner 8 mm too low *whatever* the platform's width. So the arm goes straight
  up first and makes exactly one 45° run inboard; a Ø7 rod at 45° carries its lower edge
  2.47 mm below and inboard of its axis, which puts that edge at `z = knee + 2.06` as it
  crosses |y| = 31, so the knee goes at 48 and the solid clears by 1 mm. `gps_clear()`
  intersects the real part with the real envelope on every build and prints `gps clear:`,
  the same way `interference()` prints `body clear:`.
- From the knee, 45° buys exactly as much inboard reach as it buys height, so the
  platform's **half-width and its height are one number**: land the arms at |y| = 24 and
  the underside has to be at 48 + 14 = 62, seat at 65. Widen it and the mast gets shorter
  and heavier; narrow it and it gets taller and lighter.
- The board is strapped by two ties at x = ±14 — outside the 25 mm patch, so the tie bears
  on bare PCB and not on ceramic — and it is *located* by them: the four slots sit hard
  against its long edges, so each tie's rising leg is a post at the edge. Its ends are held
  by nothing on purpose: the 4-pin header leaves one and the u.FL lead the other, and which
  is which depends on the board variant, of which there are several wearing the same
  silkscreen. The patch keeps its own tape, as shipped — a pocket for it would only fit one
  of those variants.
- **What it costs the antenna, honestly.** The L2 blocks the forward sky below ~27°
  elevation and the Pi's connector row takes a little more: this is a receiver with a mask
  over one azimuth sector, not a survey antenna. Nothing about the robot can fix that — the
  other sensor is bigger and it was here first. What the height *does* buy is 28 mm of
  separation from the Pi, which is the part that matters at 1.575 GHz.
- **It prints upside down, on the platform's top face**, and that is why nothing stands
  proud of that face. The platform is the part's one big flat — ~2000 mm² of it, against
  two Ø9.6 pads — and the right way up it is a 40 × 52 ceiling 33 mm above the bed, so
  it would print on two little discs and then want support under a table top. Inverted
  there is nothing to support: the arms grow out of the platform at 45°, the vertical run
  is vertical, the pads end up on top. `tools/orient_scan.py`: **2018 mm² of bed against
  126 the right way up, and 339 mm² of overhang against 2238** — what is left is the two
  Ø9.6 pad tops. The rest is what the 1.2 mm capture rails this part used to carry cost:
  they turned the bed face into two 40 × 1.6 strips.
- The whole mount is optional — leave it off and put the two M3 × 12 back.

## Printed BOM

| part | qty | material / settings |
|---|---|---|
| `chassis_bottom` | 1 | PETG/ASA, 4 walls, 30 % gyroid |
| `chassis_top` | 1 | PETG/ASA, 4 walls, 25 % |
| `lidar_mount` | 1 | PETG/ASA, 4 walls, 30 % |
| `gps_mount` | 1 | PETG/ASA, 4 walls, 30 % — platform down, no support |
| `camera_mount` | 1 | PETG/ASA, 4 walls, 40 % — back skirt down |
| `hip_bracket_A` / `_B` | 2 + 2 | PETG/ASA/PA-CF, 5 walls, 40 % |
| `thigh_A` / `_B` | 2 + 2 | PETG/ASA/PA-CF, 5 walls, 40 % |
| `shin_A` / `_B` | 2 + 2 | PETG/ASA/PA-CF, 5 walls, 40 % |
| `foot` | 4 | TPU 95A, 3 walls, 25 % |
| `servo_gauge` | 1 | **print this first** |

`_A` = FL + RR, `_B` = FR + RL (the two chiralities; front/rear are the same part rotated).

Sliced in OrcaSlicer 2.4.2 for an Elegoo Neptune 4 Max (0.4 nozzle, 0.2 mm layer, 0.8 mm
line, gyroid) at the settings above — G-code and the per-part figures are in
`out/gcode/` (`summary.json`), the projects are `smalldog_{body,legs,feet_tpu}.3mf`:

| plate | part | support | total | time |
|---|---|---|---|---|
| body — chassis, deck, LiDAR mount, gauge (PETG) | 387 g | 89 g | 476 g | 14 h 37 |¹
| legs — 4 hips, 4 thighs, 4 shins (PETG) | 432 g | 172 g | 604 g | 21 h 13 |
| feet — 4 × (TPU 95A) | 26 g | 2 g | 28 g | 2 h 00 |
| **whole robot** | **845 g** | **263 g** | **1108 g** | **37 h 51** |

¹ Sliced before the LiDAR pedestal was rebuilt on the measured L2 interface and tilted.
That part's solid volume went 41.7 → 36.8 cm³ and the deck lost the ⌀22 cable hole, i.e.
**about −6 g on the body plate** — the row is stale by that much until it is re-sliced.
Nothing else on the plate moved.

Supports are `normal(auto)`, 30° threshold, 0.2 mm z-gap. The parts are thin-walled, so
the walls — not the infill — set the mass: the measured fill factors run 0.78–0.97 of
solid (0.65 for the TPU feet), and that is what `PRINT_FILL` in `mini_dog.py` section 4
carries. Re-slice and re-measure after any wall-count or line-width change.

STLs in `out/stl/` are already rotated into their print orientation and sit on Z = 0.
That orientation is `PRINT_ORIENT` in `mini_dog.py`, and it is measured, not chosen by eye:
`tools/orient_scan.py` reports unsupported area, height and bed contact per build direction,
and all six axis-aligned directions were then sliced. Two things came out of it and both are
in the table above — the deck and the LiDAR pedestal were being printed upside down (the
deck's flat face had 263 mm² on the bed against 9212 mm² the other way up), worth 22 g of
support and 40 min. The rest is already at its optimum, and for `hip_bracket` / `thigh` /
`shin` that optimum is also the *strongest* build direction (`fea.py --all --orient` scores
the traction on the layer plane): the hip bracket printed on its Y face would save 17 g of
support for a third of its interlayer margin — 5.35 → 3.53 — so it stays where it is.
Support mass here is set by the parts' own internal ceilings, not by the threshold angle:
30°, 45° and 55° slice identically.

Splitting a part to print it flat was tried and measured, and it does not pay. Almost all
of the tray's support sits under the four `roll_module()` cradles — the supported ceilings
are at z = −15.36, −12.36 and +12.36, which are that cantilevered box's own shelves — and a
tray without them drops from 15950 mm² of unsupported area to 853, i.e. from 71 g of support
to 6. But the cradles bring it back: printed on their own, best case (build direction +X, so
the sleeve bore stands vertical and the shelves stand on edge) they still want 19 g for four,
and the cut adds ~31 g of new perimeter. Net **347 g against 361 g and 9 h 35 against
10 h 42** — before any bolt flange exists. The saving is print *risk*, not material, and it
buys it with four bolted joints in the primary load path, each carrying a 36 N landing
reaction and the roll servo's 2.94 N·m on a 27 mm arm. Not taken. The leg forks are not
splittable at all: the fork is what puts the servo shaft in double shear.

## Fasteners

| | qty |
|---|---|
| M2.5 × 6 (fork → driven hub, 4 per joint) | 48 |
| M2.5 × 7 (fork → passive hub, 4 per joint) | 48 |
| M3 × 10 + M3 nut (sleeve thrust clamp, 2 per joint — **not longer**) | 24 |
| M3 × 12 + M3 nut (deck → tray bosses) | 8 |
| M3 × 16 + M3 nut (LiDAR pedestal → deck) | 4 |
| M3 × 12 (Unitree L2 → pedestal, into the L2's own M3 threads) | 4 |
| M3 × 24 (LiDAR guard + deck → tray boss, replaces the front two deck screws) | 2 |
| M3 × 24 (GPS mast + deck → tray boss, replaces the rear two deck screws) | 2 |
| cable tie, 2.5 mm (GPS receiver → platform) | 2 |
| M3 × 16 + M3 nut (foot → shin ankle) | 4 |
| M2.5 × 10 + M2.5 nut (Orange Pi) | 4 |

**Nothing threads into plastic anywhere on the robot.** Every screw above that is not going
into the stock aluminium lands in a nut, and every one of those nuts sits in a side-loaded
slot (`nut_slot()` in the script) whose two walls hold the nut's flats so it cannot turn.
The slot always opens toward the face that is still reachable at the point in the assembly
order where that nut goes in — outward for the LiDAR legs, toward the middle of the tray
for the corner deck bosses, along +x for the mid pair (inboard of them is the pack),
outboard for the Orange Pi standoffs, and above the foot's top face for the ankle bolt. No heat-set inserts.

No nuts at the hubs: the ⌀2.5 holes in both aluminium plates are tapped M2.5, so the screw
goes in from the outside of the fork arm and threads into metal. There is no room for a nut
anyway — 0.30 mm behind the driven hub, and the 0.55 mm base recess behind the passive one.
Do not fit longer screws: past the hub they bottom out on the servo case, which the vendor
FAQ says stalls and burns the servo.

## Assembly order

1. Print `servo_gauge`, check a real ST3215 slides into the sleeve and the ⌀14 pattern lines up.
2. Set servo IDs 1…12 over the bus **before** assembly (URT-1 + Feetech tool).
3. Per joint: two M3 nuts into the thrust lug's channels → slide the servo into the sleeve
   → run both thrust bolts in, evenly, until the case stops rocking → fit both aluminium
   hubs to the servo → bolt the fork arms to the hubs (bottom arm's ⌀23 pad enters the
   case-base recess). The thrust bolts come first: once the fork is on, its spine sweeps
   over the lug.
4. Legs: hip bracket → thigh → shin → press the TPU foot onto the ⌀18 spigot, then the M3
   up through the foot into the nut in the ankle slot. The slot sits above the foot's top
   face, so the nut goes in with the foot already fitted, and the foot stays removable.
5. Chassis, and the order matters because half these nuts stop being reachable later:
   8 M3 nuts into the tray bosses' slots (the corner pair's open toward the middle of the
   tray, the mid pair's along +x — inboard of those two is the battery bay) →
   XT60 and XT30 into their rear-panel pockets from inside, balance lead out through its
   pass-through (all three are trapped by the deck afterwards) →
   battery + BMS in the tray, servo bus routed out through the side ports →
   4 M3 nuts into the pedestal legs' lower slots and bolt the pedestal to the deck **from
   underneath, with the deck still off the tray** → deck onto the tray →
   4 M2.5 nuts into the standoff slots, Orange Pi, ESP32/URT-1 →
   GPS mast onto the rear pair of deck screws, swapping them for M3 × 24, then the
   receiver into its rails and two ties (it arches over the Pi, so the Pi goes in first
   and the mast has to come off again to lift the Pi out) →
   camera: 2 M3 nuts into the gusset's slots (they open forward, under the chin) →
   channel onto the ledge, tongue into its pocket, both M3 down → slide the module in from
   the +y end, connector end last. The module comes out without touching anything else,
   which is the point of a channel rather than a bolted plate →
   L2 onto the flange, 4 × M3 × 12 up into its own tapped holes. No nut there — the L2's
   base is tapped M3 6 mm deep, and 12 mm is 7 mm of flange plus 5 mm of thread. Do not
   substitute a longer screw: it bottoms in the blind hole and lifts the sensor off its
   seat without ever feeling loose.
6. Zero all joints at the mechanical zero pose (legs straight down) before powering the gait.

## Onshape import

- `out/mini_dog_assembly.step` — the whole robot, one named instance per part plus servo
  dummies, already in robot coordinates. Import as **Assembly**; every solid keeps its name.
- `out/step/<part>.step` — individual parts if you want to rebuild features on top of them.
- Onshape imports STEP as dumb solids (no feature history). That is expected here: the
  history lives in `mini_dog.py`. Change a parameter there and re-import, or use the
  imported solid as a base and add Onshape features on top.
- Units are mm; no scaling needed.

## Simulation export - ROS 2 and MuJoCo

[export_sim.py](export_sim.py) writes `out/sim/` from the same parametric model:

> There is a **second** exporter of this CAD: `../ros2/smalldog_description/scripts/
> generate_model.py` builds the ROS 2 package's own description (different link
> decomposition, its own collision primitives) by importing the same `mini_dog.py`.
> Change the model and both must be re-run — see step 6 of *Verifying a change* in
> [CLAUDE.md](CLAUDE.md). All masses and densities come from `mini_dog.py` section 4;
> no downstream file keeps a copy.

| file | what it is |
|---|---|
| `mini_dog.urdf` | ROS 2 URDF. Meshes are `package://mini_dog_description/meshes/...`; pass `--mesh-uri relative` for non-ROS tools. |
| `mini_dog.xml` | MuJoCo MJCF: flat floor, free-floating base, 12 position actuators, IMU site, LiDAR site, a renderable `<camera>`, `stand` keyframe. |
| `mini_dog_terrain.xml` | the same robot on rough ground - the plane swapped for a heightfield. |
| `meshes/*.stl` | the printed solids, in link-local frames, still in mm (both files scale by 0.001). |
| `meshes/terrain.png` | that heightfield, 8 x 8 m, written by [terrain.py](terrain.py). |
| `camera_view.png` | one frame rendered from the model's own `<camera>` by `--check` - written only when Pillow is installed. |

Both MJCFs also carry the LiDAR: a `lidar` site at the L2's optical centre and the scan
parameters as `<custom>` numerics, written by [lidar.py](lidar.py) — see *The LiDAR, in
simulation* below. The URDF gets the matching `lidar_link`.

The GPS gets a frame and nothing more: a `gps` site at the patch's phase centre
(`mini_dog.gps_pose()`, 20 mm up the mast from the platform) plus the receiver drawn as a
visual box, and a `gps_link` in the URDF — +Z is the patch normal, which is why the
platform is level and why there is no `rpy` on that joint. There is no simulated fix: a
NavSatFix would be a noise model, not a measurement, and nothing in the workspace consumes
one yet.

- SI out: metres, kilograms, radians. Frame is mini_dog's - +X forward, +Y left, +Z up,
  origin at the chassis centre in the hip-roll plane. Zero pose = legs straight down.
- Joint axes are physical and identical on all four legs: `hip_roll` about +X,
  `hip_pitch` / `knee` about +Y. The mirrored legs carry **mirrored limits**, not mirrored
  axes, so a positive `hip_pitch` swings every leg the same way in world terms.
- Link masses and inertias are integrated from the real solids at each part's **measured**
  print density (`PRINT_FILL` in `mini_dog.py` section 4 — filament mass off the sliced
  plate over solid volume, feet in TPU, everything else PETG), plus a real ST3215 solid at
  its catalogue mass on each joint, plus the battery, the electronics and the LiDAR as
  boxes. Total **2.496 kg**, cross-checked against `fea.robot_mass()` (2.480 kg — the
  difference is `servo_gauge`, a test print that is not part of the robot) and against the
  ROS 2 description generator, which reports 2.495 kg.
- Joint limits come from the ROM scan in `out/bom.json` (coarse 10 deg); `--rom-step 2`
  re-scans them. `hip_roll` currently reports +-90 deg, which is the scan's own window
  rather than an interference limit - the export flags that instead of hiding it.
- Visual = the printed meshes; collision = primitives (chassis box, leg capsules, foot
  spheres), because a mesh collider of a lightened bracket is slow and, in MuJoCo, silently
  replaced by its convex hull.
- **Estimates, not measurements:** the MuJoCo joint damping / armature / friction-loss and
  the position-servo gain. The ST3215 gearbox is a black box; tune them against hardware.

### Rough ground

[terrain.py](terrain.py) generates the heightfield both exporters use: seeded fractal value
noise, +-27 mm over a 160 mm wavelength (36 deg peak slope), flat only inside 160 mm of the
origin - the stance footprint, so the robot spawns level - and fully rough 250 mm further
out. Same seed, same ground, so a run on terrain stays comparable to the last one. Keep
that pad tight: a wide one puts the robot in the middle of a parking lot and the viewer,
which opens looking straight at it, shows what looks like a flat floor.

The field also carries an obstacle course - ramps, walls and logs, bedded into the relief
along +x (`COURSE` in [terrain.py](terrain.py)). The heightfield alone is smooth by
construction, so a foot always lands on a hillside and never meets an edge; the course is
the part the noise cannot express. Graded to what the trot can actually clear, measured
one obstacle at a time on flat ground against 1252 mm of clear ground: ramps to at least
14 deg, walls to ~18 mm, logs to ~22 mm. The wall cliff between 18 and 22 mm is the 22 mm
foot swing exactly.

It starts at x = 0.95 m, past the 0.66 m the 5 s regression trot reaches, so that
measurement stays a measurement of the relief. `--no-terrain-obstacles` on either exporter
leaves it out; `../ros2/tools/standalone_sim.py --course` walks it. A rotation there is
written as a quaternion, not euler - both exporters compile their MJCF with
`<compiler angle="radian">`, which reads a degree value as radians without complaining.

`--terrain-amp <mm>` and `--terrain-wave <mm>` on either exporter move it. Distance on the
5 s headless trot at 0.20 m/s, against 793 mm on the flat:

| | 260 mm wave | 160 mm wave |
|---|---|---|
| **+-18 mm** | 584 mm | 699 mm |
| **+-27 mm** | 508 mm | **634 mm** (default) |
| **+-40 mm** | - | 474 mm |

Nothing in that table fell over or veered off. Two results that look wrong and are not: the
default relief is **taller than the 22 mm foot swing** - the slopes are smooth, so a foot
lands on a hillside instead of stubbing into a step - and long wavelengths cost more
distance than short ones, because a 260 mm hill is a climb while a 160 mm one is a bump.
The gait is blind either way; push the amplitude far enough and it is the controller that
gives out, not the ground.

### The LiDAR, in simulation

[lidar.py](lidar.py) puts the **L2 in the model as a sensor**, not just as 230 g of
payload. It writes a `<site name="lidar">` at the sensor's optical centre — the pose comes
from `mini_dog.lidar_pose()` and `LIDAR_OPT`, so it leans with `LIDAR_TILT` and moves when
the pedestal does — and a `<custom><numeric name="lidar_*">` block carrying the scan
parameters into the compiled model. `Scanner` then ray-casts it with `mj_multiRay`.

The parameters go **inside the model file** on purpose. The other consumer is C++
(`mujoco_ros2_control`, in the ROS 2 workspace) and cannot import this module, so the
compiled `mjModel` is the interface: the numbers exist once, in `mini_dog.py` section 3's
LiDAR block, and both readers pick them out of the file they already load. A model without
those numerics has no LiDAR, and both readers say so rather than inventing a cone.

| | |
|---|---|
| cone | half-angle 96° about the sensor axis (`LIDAR_FOV_NEGA`) — the "360 × 90" plus NEGA |
| rate | 21600 points/s → 2160 per frame at 10 Hz **verify** |
| range | 0.05 … 30 m, 20 mm 1σ range noise **verify** |
| frame | `lidar_link`: +Z **is** the sensor's axis, so every FOV angle in the CAD reads directly |
| sees | what the physics collides with (geom groups ≠ 2), minus the body it is bolted to |

The scan pattern is a **Risley pair** — two counter-rotating wedge prisms, each deflecting
by half the cone — which is the standard way to build a *non-repetitive* scan: stand still
and the field keeps filling in instead of retracing rings. It reproduces the L2's coverage,
its point rate and that non-repetition. It does **not** reproduce the density profile: a
rosette piles points up on the axis and at the rim, while the manual says the real sensor
is densest at the middle of its vertical FOV, which is the very thing `LIDAR_TILT = 45°`
was chosen to exploit. Use the model for geometry, coverage and occlusion; do not use it to
argue about how many returns an object gets. No intensity is published either — MuJoCo
returns a distance and a geom id and nothing about the surface, and a plausible-looking
invented reflectance is worse than none.

`--check` reports one frame, from the robot standing on the flat floor:

```
lidar  2160 rays/frame at 10 Hz -> 564 returns, 564 of them ground, 0 the robot itself;
       range 0.33..5.30 m
       ground on the centreline from x +282 mm (+198 mm past the leading foot) out to +1249 mm
```

Three quarters of the rays are the sky — the cone is a hemisphere and only the lower part
of it meets the floor — and the near edge on the centreline is the number the tilt exists
to buy. It is the same quantity the parameter block argues about (147 mm past the leading
foot at the CAD stance); the value here is a little further out because the stand pose is
taller and because 2160 points sample that rim sparsely.

In the ROS 2 workspace the same sensor is published as a `PointCloud2` — see
[../ros2/README.md](../ros2/README.md), *LiDAR*.

`--check` loads both files in MuJoCo, drops the robot into its stand pose and reports base
height, feet on the ground and whether the two files agree on mass; it then stands the same
robot on `mini_dog_terrain.xml` and reports that separately, and finishes with the LiDAR
frame above.

## Verify before printing the whole set

1. `servo_gauge` fit — the one thing that can invalidate everything else.
2. Orange Pi 5 Pro hole pattern (currently 92 × 54).
3. ~~Unitree L2 base bolt circle~~ — measured from the manual drawing: ⌀51, 4 × M3 ▽6 at
   22.5°, ⌀60 spigot, ⌀75 base, 75 × 75 × 65 mm, 230 g. Cable exit still to confirm on a
   real unit (the L2 has three: DC 3.5×1.35, RJ45 and GH1.25-4P).
4. BMS outline (currently 64 × 27 × 13), and the three connector bodies in the rear panel:
   XT60 16.5 × 8.5, XT30 12.0 × 6.6, JST-XH 3S plug 13.0 × 6.0. All four are catalogue
   numbers, not measurements — check them against the parts in your hand before printing
   the tray, because a pocket that is 0.3 mm small is a connector that does not go in.
5. Servo cable/connector envelope: the sleeve window is 15 × 12 mm at the far end face.
6. Orange Pi stack envelope `OPI_BOX` (currently 100 × 62 × 20 over the deck) — it is now
   a keep-out, not just a mass box: `gps_mount` is shaped around it.
7. GY-NEO6MV2 board (currently 36 × 26 × 1.6), its active patch (25 × 25 × 8) and the
   22 g the vendor quotes for the pair. It is a bazaar part, not a documented one, and
   several boards share the silkscreen — measure the one in your hand, especially where
   the header and the u.FL sit.

## Known limitations of V1

- Stance is tall (~160 mm ground clearance at the nominal pose). Good for snow, less good
  for tipping — crouch harder in the gait, or shorten `L_THIGH` / `L_SHIN`.
- The thigh is still a plain constant-section box between its two joints. That matches
  what the reference legs do there, but its corners are unblended, unlike the shin's.
- No IMU mount, no shell/covers, no connector strain reliefs yet.
- The ROM scan is a 10° sweep; refine with `step=2` before committing to joint limits.
- The Waveshare `ROBOTIC DOG.step` supplies the **shape** of the shin and nothing else.
  Its joint spacing does not transfer — it is an aluminium-plate design with a single-shear
  servo horn, and this robot is a printed sleeve-and-fork one.

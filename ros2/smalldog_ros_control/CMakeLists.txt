cmake_minimum_required(VERSION 3.16)
project(smalldog_ros_control)

find_package(ament_cmake REQUIRED)

install(
  DIRECTORY launch config
  DESTINATION share/${PROJECT_NAME}
)
install(
  DIRECTORY description/urdf
  DESTINATION share/${PROJECT_NAME}
)

ament_package()
